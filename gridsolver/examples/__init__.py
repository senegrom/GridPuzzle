"""Built-in example puzzles.

``gridpuzzle --example`` reads them through ``catalog.get_example``;
``gridpuzzle --module gridsolver.examples.sudoku`` (or ``futoshiki``,
``killer_sudoku``, ``miracle_sudoku``) solves the grid ``g`` a module builds
when it is imported. The puzzle corpora in the repository's Examples/
directory are data files for the tests and ``--file``; they are not shipped.
"""
