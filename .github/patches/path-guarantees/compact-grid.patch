diff --git a/gridsolver/grid_classes/compact_grid.py b/gridsolver/grid_classes/compact_grid.py
index 70152fc..f135822 100644
--- a/gridsolver/grid_classes/compact_grid.py
+++ b/gridsolver/grid_classes/compact_grid.py
@@ -44,9 +44,10 @@ class CompactGrid(Grid):
     Board puzzles with blocked cells and edge puzzles do not naturally map to a
     dense rectangular value grid.  CompactGrid keeps the solver-facing cells in
     one row and supplies a stable key <-> cell mapping for rendering and APIs.
-    The specialised puzzle rules provide the primary propagation. Generic
-    all-different, tuple, forcing, and trial deductions remain available,
-    while geometry-specific Sudoku patterns are excluded.
+    Specialised puzzle rules provide the primary propagation.  Each subclass
+    selects a :class:`TechniqueProfile`; generic tuple and contradiction
+    techniques run only for profiles that explicitly enable them, while
+    geometry-specific Sudoku patterns remain excluded from compact grids.
     """
 
     technique_profile = TechniqueProfile.GENERIC
