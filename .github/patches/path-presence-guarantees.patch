diff --git a/benchmarks/path_presence_guarantees_2026-08-11.md b/benchmarks/path_presence_guarantees_2026-08-11.md
new file mode 100644
index 0000000..209aacc
--- /dev/null
+++ b/benchmarks/path_presence_guarantees_2026-08-11.md
@@ -0,0 +1,42 @@
+# Path presence guarantees and permutation-rule debloat — 2026-08-11
+
+Hidato and Numbrix have one variable for every value in `1..N`. Their models
+now install the `N` whole-path presence guarantees during construction rather
+than relying on a one-shot `ElementsAtLeastOnce` rule to emit them during the
+first propagation pass.
+
+The first direct implementation was rejected: merely pre-seeding guarantees
+while retaining separate whole-grid at-most-once and path rules changed the
+initial rule order and made the measured Numbrix expert solve about 14% slower.
+The accepted design folds the whole-grid at-most-once invariant into
+`ConsecutiveAdjacencyRule`, keeps guarantee filtering independent so guarantee
+shrinks do not wake the expensive graph rule unnecessarily, and removes direct
+final-solution checks for presence guarantees already implied by a complete
+all-different constraint. Every solve used `depth_gate=None`.
+
+Measurements used CPython 3.13.5 locally because the development container did
+not include 3.14. GitHub CI remains the Python 3.14 authority. Each comparison
+used alternating baseline/candidate order and exact deterministic solution
+fingerprints.
+
+| Case | Baseline median | Candidate median | Change |
+|---|---:|---:|---:|
+| Construct ten 10x10 Numbrix grids | 0.002459 s | 0.003024 s | +22.97% |
+| Propagate ten blank 6x6 Numbrix grids | 0.002007 s | 0.001750 s | -12.79% |
+| Solve ten small retained Hidato instances | 0.001448 s | 0.001310 s | -9.51% |
+| Solve retained Numbrix expert instance | 0.058721 s | 0.055376 s | -5.70% |
+| Solve loaded 4x4 Sudoku four times | 0.059516 s | 0.058841 s | -1.13% |
+
+The construction percentage is an intentional shift of work from the first
+propagation pass to model creation and is roughly 0.0006 seconds in absolute
+terms for a 100-cell path. The subsequent propagation and solve measurements
+recover more than that cost. The retained Hidato and Numbrix solution hashes
+matched exactly; the loaded Sudoku hash was
+`484be136bb2b590f81c4b23096dc1af609324b9280fa83b61969618fc785a89c` in
+both trees.
+
+Correctness coverage includes immediate guarantee availability, guarantee-only
+propagation before a rule pass, the integrated permutation invariant,
+independent path solution oracles, malformed-source validation, clone/pickle
+behavior inherited from the existing guarantee suite, and the permanent
+rule-metadata guard.
+diff --git a/gridsolver/grid_classes/path_puzzles.py b/gridsolver/grid_classes/path_puzzles.py
+index 3893471..354a547 100644
+--- a/gridsolver/grid_classes/path_puzzles.py
++++ b/gridsolver/grid_classes/path_puzzles.py
+@@ -6,7 +6,7 @@ from numbers import Integral
+ from gridsolver.abstract_grids.grid import Grid, TechniqueProfile
+ from gridsolver.grid_classes.compact_grid import CompactGrid, _rectangular_rows
+ from gridsolver.rules.topology import ConsecutiveAdjacencyRule
+-from gridsolver.rules.unique import ElementsAtLeastOnce, ElementsAtMostOnce
++from gridsolver.rules.unique import value_presence_guarantees
+ 
+ 
+ type BoardCell = tuple[int, int]
+@@ -115,15 +115,22 @@ class _ConsecutivePathGrid(CompactGrid):
+         self.adjacency = tuple(adjacency)
+ 
+         cells = tuple(range(self.len))
+-        self.add_rules_checked(
+-            (
+-                ElementsAtMostOnce(self, cells=cells),
+-                ElementsAtLeastOnce(self, cells=cells),
+-                ConsecutiveAdjacencyRule(
+-                    self,
+-                    cells=cells,
+-                    adjacency=self.adjacency,
+-                ),
++        # Every path value must occur somewhere from the moment the model is
++        # created.  Previously a one-shot ElementsAtLeastOnce rule emitted
++        # these guarantees only during the first propagation pass.
++        self.add_gtees_checked(
++            value_presence_guarantees(
++                self.rows,
++                self.cols,
++                self.max_elem,
++                cells,
++            )
++        )
++        self.add_rule_checked(
++            ConsecutiveAdjacencyRule(
++                self,
++                cells=cells,
++                adjacency=self.adjacency,
+             )
+         )
+ 
+diff --git a/gridsolver/rules/topology.py b/gridsolver/rules/topology.py
+index b87742a..5bce95f 100644
+--- a/gridsolver/rules/topology.py
++++ b/gridsolver/rules/topology.py
+@@ -6,10 +6,11 @@ from numbers import Integral
+ 
+ from gridsolver.abstract_grids.gridsize_container import GridSizeContainer
+ from gridsolver.rules.rules import Guarantee, InvalidGrid, Rule, RuleAlwaysSatisfied
++from gridsolver.rules.unique import ElementsAtMostOnce
+ 
+ 
+-class ConsecutiveAdjacencyRule(Rule):
+-    """Require consecutive values to occupy adjacent cells.
++class ConsecutiveAdjacencyRule(ElementsAtMostOnce):
++    """Require a permutation whose consecutive values occupy adjacent cells.
+ 
+     In addition to local predecessor/successor support, the rule uses graph
+     distances from fixed clues. A candidate ``v`` at cell ``c`` is impossible
+@@ -18,6 +19,11 @@ class ConsecutiveAdjacencyRule(Rule):
+     is also enforced.
+     """
+ 
++    # Presence guarantees are filtered independently.  Consuming every
++    # guarantee here would wake this expensive graph rule after each guarantee
++    # shrink; the permutation invariant already prevents duplicate values.
++    uses_guarantees = False
++
+     __slots__ = (
+         "adjacency",
+         "_adjacency_by_cell",
+@@ -36,7 +42,11 @@ class ConsecutiveAdjacencyRule(Rule):
+         if len(raw_cells) != len(raw_adjacency):
+             raise ValueError("Adjacency must contain one entry per rule cell")
+ 
+-        super().__init__(gsz, raw_cells, None)
++        # Preserve the caller's cell/adjacency pairing.  The inherited
++        # at-most-once implementation needs only the normalized Rule state;
++        # calling Rule directly avoids ElementsAtMostOnce sorting the cells
++        # without applying the same permutation to adjacency.
++        Rule.__init__(self, gsz, raw_cells, None)
+         if self.len_cells != self._max_elem:
+             raise ValueError(
+                 "Consecutive adjacency requires one cell for every domain value"
+@@ -208,18 +218,21 @@ class ConsecutiveAdjacencyRule(Rule):
+         candidates: tuple[set[int], ...],
+         guarantees: Iterable[Guarantee] | None = None,
+     ) -> tuple[bool, None, None]:
+-        fixed: dict[int, int] = {}
+-        for cell in self.cells:
+-            value = known[cell]
+-            if value <= 0:
+-                continue
+-            if not 1 <= value <= self._max_elem:
+-                raise InvalidGrid()
+-            previous = fixed.get(value)
+-            if previous is not None and previous != cell:
+-                raise InvalidGrid()
+-            fixed[value] = cell
++        # A consecutive path over N cells is also a permutation of 1..N.
++        # Keep that invariant in this rule instead of making every path grid
++        # carry a second whole-grid ElementsAtMostOnce propagator.
++        if any(
++            known[cell] < 0 or known[cell] > self._max_elem
++            for cell in self.cells
++        ):
++            raise InvalidGrid()
++        self._process_new_candidate_cells(known, candidates)
+ 
++        fixed = {
++            known[cell]: cell
++            for cell in self.cells
++            if known[cell] > 0
++        }
+         fixed_items = tuple(sorted(fixed.items()))
+         for index, (first_value, first_cell) in enumerate(fixed_items):
+             for second_value, second_cell in fixed_items[index + 1 :]:
+diff --git a/gridsolver/rules/unique.py b/gridsolver/rules/unique.py
+index 8cd7396..06c3157 100644
+--- a/gridsolver/rules/unique.py
++++ b/gridsolver/rules/unique.py
+@@ -1,9 +1,39 @@
+ from collections.abc import Iterable, MutableSequence
++from functools import lru_cache
+ 
+ from gridsolver.abstract_grids.gridsize_container import GridSizeContainer
+ from gridsolver.rules.rules import Guarantee, IdxType, InvalidGrid, Rule, RuleAlwaysSatisfied
+ 
+ 
++@lru_cache(maxsize=256)
++def _cached_value_presence_guarantees(
++    rows: int,
++    cols: int,
++    max_elem: int,
++    cells: tuple[int, ...],
++) -> tuple[Guarantee, ...]:
++    cell_set = frozenset(cells)
++    return tuple(
++        Guarantee(value, cell_set, rows, cols)
++        for value in range(1, max_elem + 1)
++    )
++
++
++def value_presence_guarantees(
++    rows: int,
++    cols: int,
++    max_elem: int,
++    cells: Iterable[int],
++) -> tuple[Guarantee, ...]:
++    """Return cached immutable guarantees requiring every domain value."""
++    return _cached_value_presence_guarantees(
++        rows,
++        cols,
++        max_elem,
++        tuple(sorted(cells)),
++    )
++
++
+ class ElementsAtMostOnce(Rule):
+     __slots__ = ()
+     uses_guarantees = True  # _update_from_guarantees (and SaEAMO's cage filter)
+@@ -49,7 +79,10 @@ class ElementsAtMostOnce(Rule):
+     ) -> None:
+         unknown_cells = frozenset(new_candidate_cells)
+         for guarantee in guarantees:
+-            if guarantee.cells <= unknown_cells:
++            # Equal cell sets cannot eliminate anything.  Using a strict
++            # subset also avoids work for whole-domain presence guarantees
++            # that are intentionally installed before path propagation.
++            if guarantee.cells < unknown_cells:
+                 for cell in unknown_cells - guarantee.cells:
+                     candidates[cell].discard(guarantee.val)
+                     if not candidates[cell]:
+@@ -105,8 +138,10 @@ class ElementsAtLeastOnce(Rule):
+         known: MutableSequence[int],
+         candidates: tuple[set[int], ...],
+         guarantees: Iterable[Guarantee] | None = None,
+-    ) -> tuple[bool, list[Rule], list[Guarantee]]:
+-        return False, [], [
+-            Guarantee(value, frozenset(self.cells), self._rows, self._cols)
+-            for value in range(1, self._max_elem + 1)
+-        ]
++    ) -> tuple[bool, tuple[Rule, ...], tuple[Guarantee, ...]]:
++        return False, (), value_presence_guarantees(
++            self._rows,
++            self._cols,
++            self._max_elem,
++            self.cells,
++        )
+diff --git a/gridsolver/solver/validation.py b/gridsolver/solver/validation.py
+index 5259342..2b3c42f 100644
+--- a/gridsolver/solver/validation.py
++++ b/gridsolver/solver/validation.py
+@@ -51,6 +51,8 @@ class _FallbackBudget:
+ def _canonical_guarantee(
+     guarantee: object,
+     plan: _ValidationPlan,
++    *,
++    cell_cache: dict[int, tuple[object, frozenset[int]]] | None = None,
+ ) -> Guarantee:
+     if type(guarantee) is not Guarantee:
+         raise TypeError(
+@@ -84,10 +86,24 @@ def _canonical_guarantee(
+         raise ValueError(
+             f"Guarantee value {value} is outside 1..{plan.max_elem}"
+         )
+-    if isinstance(guarantee.cells, (str, bytes, bytearray)):
++    raw_cell_container = guarantee.cells
++    # Cache only the exact immutable container used by normal Guarantee
++    # objects.  A hashable extension container may still be mutable or expose
++    # side effects, so revalidate it rather than reusing stale normalized cells.
++    cache_key = (
++        id(raw_cell_container)
++        if type(raw_cell_container) is frozenset
++        else None
++    )
++    if cell_cache is not None and cache_key is not None:
++        cached = cell_cache.get(cache_key)
++        if cached is not None and cached[0] is raw_cell_container:
++            return Guarantee(value, cached[1], rows, cols)
++
++    if isinstance(raw_cell_container, (str, bytes, bytearray)):
+         raise TypeError("Guarantee cells must be an iterable of integers")
+     try:
+-        raw_cells = tuple(guarantee.cells)
++        raw_cells = tuple(raw_cell_container)
+     except TypeError as exc:
+         raise TypeError(
+             "Guarantee cells must be an iterable of integers"
+@@ -105,7 +121,10 @@ def _canonical_guarantee(
+                 f"Guarantee cell {cell} is outside 0..{plan.length - 1}"
+             )
+         cells.add(cell)
+-    return Guarantee(value, frozenset(cells), rows, cols)
++    canonical_cells = frozenset(cells)
++    if cell_cache is not None and cache_key is not None:
++        cell_cache[cache_key] = (raw_cell_container, canonical_cells)
++    return Guarantee(value, canonical_cells, rows, cols)
+ 
+ 
+ def _canonical_guarantee_is_satisfied(
+@@ -425,13 +444,21 @@ def _build_validation_plan(source: Grid) -> _ValidationPlan:
+                 f"Malformed rule in source grid: {rule!r}: {exc}"
+             ) from exc
+ 
++    cell_cache: dict[int, tuple[object, frozenset[int]]] = {}
++
+     def canonicalize_guarantees(
+         raw_guarantees: tuple[Guarantee, ...],
+     ) -> tuple[Guarantee, ...]:
+         canonical: list[Guarantee] = []
+         for guarantee in raw_guarantees:
+             try:
+-                canonical.append(_canonical_guarantee(guarantee, partial))
++                canonical.append(
++                    _canonical_guarantee(
++                        guarantee,
++                        partial,
++                        cell_cache=cell_cache,
++                    )
++                )
+             except (TypeError, ValueError) as exc:
+                 raise InvalidSolutionError(
+                     f"Malformed guarantee in source grid: {guarantee!r}: {exc}"
+@@ -440,6 +467,23 @@ def _build_validation_plan(source: Grid) -> _ValidationPlan:
+ 
+     active_guarantees = canonicalize_guarantees(raw_active_guarantees)
+     inactive_guarantees = canonicalize_guarantees(raw_inactive_guarantees)
++
++    # On a complete N-cell assignment over 1..N, an at-most-once rule over
++    # exactly N cells already proves that every domain value occurs there.
++    # Keep the active guarantees available to extension-rule validation, but
++    # avoid rechecking those mathematically implied presence constraints one
++    # by one in the final solution loop.
++    permutation_cells = {
++        frozenset(rule.cells)
++        for rule in validated_rules
++        if isinstance(rule, ElementsAtMostOnce)
++        and rule.len_cells == partial.max_elem
++    }
++    direct_guarantees = tuple(
++        guarantee
++        for guarantee in active_guarantees + inactive_guarantees
++        if guarantee.cells not in permutation_cells
++    )
+     return _ValidationPlan(
+         rows=partial.rows,
+         cols=partial.cols,
+@@ -449,7 +493,7 @@ def _build_validation_plan(source: Grid) -> _ValidationPlan:
+         known=partial.known,
+         candidates=partial.candidates,
+         rules=tuple(validated_rules),
+-        guarantees=active_guarantees + inactive_guarantees,
++        guarantees=direct_guarantees,
+         active_guarantees=active_guarantees,
+     )
+ 
+diff --git a/tests/test_new_puzzle_families.py b/tests/test_new_puzzle_families.py
+index 063a649..35c7a98 100644
+--- a/tests/test_new_puzzle_families.py
++++ b/tests/test_new_puzzle_families.py
+@@ -13,12 +13,14 @@ from gridsolver.abstract_grids.grid_loading import create_from_file, create_from
+ from gridsolver.grid_classes.kakuro import Kakuro
+ from gridsolver.grid_classes.path_puzzles import Hidato, Numbrix
+ from gridsolver.grid_classes.slitherlink import Slitherlink
+-from gridsolver.rules.rules import InvalidGrid
++from gridsolver.rules.rules import Guarantee, InvalidGrid
+ from gridsolver.rules.topology import (
+     ConsecutiveAdjacencyRule,
+     SingleLoopRule,
+ )
++from gridsolver.rules.unique import ElementsAtLeastOnce, ElementsAtMostOnce
+ from gridsolver.solver import atomic_solver, solver
++from gridsolver.solver.solve_guarantees import filter_guarantees
+ import run as run_cli
+ 
+ 
+@@ -147,6 +149,57 @@ def _slitherlink_oracle(grid: Slitherlink) -> set[tuple[int, ...]]:
+     return result
+ 
+ 
++def test_path_grids_start_with_cached_presence_guarantees():
++    grids = (Hidato(2, 2), Numbrix(2, 2))
++    cells = frozenset(range(4))
++    expected = {
++        Guarantee(value, cells, 1, 4)
++        for value in range(1, 5)
++    }
++
++    for grid in grids:
++        assert grid.guarantees == expected
++        assert not grid.guarantees_ia
++        assert len({id(guarantee.cells) for guarantee in grid.guarantees}) == 1
++
++        assert len(grid.rules) == 1
++        rule = next(iter(grid.rules))
++        assert type(rule) is ConsecutiveAdjacencyRule
++        assert isinstance(rule, ElementsAtMostOnce)
++        assert not isinstance(rule, ElementsAtLeastOnce)
++
++    first = {guarantee.val: guarantee for guarantee in grids[0].guarantees}
++    second = {guarantee.val: guarantee for guarantee in grids[1].guarantees}
++    assert all(first[value] is second[value] for value in first)
++
++
++def test_path_presence_guarantees_can_propagate_before_any_rule_pass():
++    grid = Numbrix(2, 2)
++    for cell in (1, 2, 3):
++        grid._candidates[cell].discard(1)
++
++    filter_guarantees(grid)
++
++    assert grid[0] == 1
++    assert not any(
++        guarantee.val == 1
++        for guarantee in grid.guarantees
++    )
++
++
++def test_consecutive_path_rule_enforces_the_permutation_invariant():
++    grid = Numbrix(2, 2)
++    rule = next(iter(grid.rules))
++    known = [1, 1, 0, 0]
++    candidates = tuple({1, 2, 3, 4} for _ in range(4))
++
++    with pytest.raises(InvalidGrid):
++        rule.apply(known, candidates)
++
++    with pytest.raises(InvalidGrid):
++        rule.apply([5, 0, 0, 0], candidates)
++
++
+ def test_numbrix_matches_independent_2x2_oracle():
+     grid = Numbrix.from_board(((1, 0), (4, 0)))
+ 
+diff --git a/tests/test_solution_validation.py b/tests/test_solution_validation.py
+index 1452f3f..8435fad 100644
+--- a/tests/test_solution_validation.py
++++ b/tests/test_solution_validation.py
+@@ -8,6 +8,7 @@ from gridsolver.rules.sumrules import SumAndElementsAtMostOnce
+ from gridsolver.rules.uneq import DiffGe2Rule, IneqRule, UneqRule
+ from gridsolver.rules.unique import ElementsAtLeastOnce, ElementsAtMostOnce
+ from gridsolver.solver import solver
++from gridsolver.solver import validation as validation_module
+ from gridsolver.solver.validation import InvalidSolutionError, validate_solution
+ 
+ 
+@@ -90,3 +91,44 @@ def test_relation_rules_are_order_independent_and_accept_no_guarantee_list():
+ 
+     with pytest.raises(ValueError, match="at least one related cell"):
+         UneqRule(grid, origin_cell=0, rel_cells=[])
++
++
++def test_full_domain_all_different_implies_matching_presence_guarantees(
++    monkeypatch,
++):
++    grid = Grid(1, 2, max_elem=2)
++    grid.add_rule_checked(ElementsAtMostOnce(grid, cells=(0, 1)))
++    grid.add_gtees_checked(
++        (
++            Guarantee(1, frozenset({0, 1}), 1, 2),
++            Guarantee(2, frozenset({0, 1}), 1, 2),
++        )
++    )
++
++    def fail_if_rechecked(guarantee, values):
++        raise AssertionError(
++            "presence guaranteed by a complete all-different was rechecked"
++        )
++
++    monkeypatch.setattr(
++        validation_module,
++        "_canonical_guarantee_is_satisfied",
++        fail_if_rechecked,
++    )
++    validation_module.validate_solution(
++        grid,
++        ImmutableGrid([1, 2], 1, 2, 2),
++    )
++
++
++def test_implied_guarantees_are_still_canonicalized_before_elision():
++    grid = Grid(1, 2, max_elem=2)
++    grid.add_rule_checked(ElementsAtMostOnce(grid, cells=(0, 1)))
++    # Bypass the public mutation boundary to model corrupt extension state.
++    grid.guarantees.add(Guarantee(1, frozenset({0, 2}), 1, 2))
++
++    with pytest.raises(InvalidSolutionError, match="Malformed guarantee"):
++        validation_module.validate_solution(
++            grid,
++            ImmutableGrid([1, 2], 1, 2, 2),
++        )
