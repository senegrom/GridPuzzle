name: Export correction source

on:
  push:
    branches:
      - agent/correct-path-guarantees-and-compat
    paths:
      - .github/workflows/export-correction-source.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/upload-artifact@v4
        with:
          name: gridpuzzle-correction-source
          path: .
          include-hidden-files: true
          retention-days: 1
