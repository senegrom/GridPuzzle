name: Finalize atomic rule rollback

on:
  push:
    branches:
      - agent/main-correctness-debloat-followup
    paths:
      - .github/patches/atomic-rule-rollback.patch
      - .github/workflows/finalize-atomic-rule-rollback.yml

permissions:
  contents: write

concurrency:
  group: finalize-atomic-rule-rollback
  cancel-in-progress: false

env:
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PYTHONHASHSEED: "0"
  PYTHONUNBUFFERED: "1"

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v6
        with:
          python-version: "3.14"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Apply the reviewed rollback patch
        shell: bash
        run: |
          set -euo pipefail
          git apply --check .github/patches/atomic-rule-rollback.patch
          git apply .github/patches/atomic-rule-rollback.patch
          rm -f \
            .github/patches/atomic-rule-rollback.patch \
            .github/workflows/finalize-atomic-rule-rollback.yml
          git diff --check

      - name: Install, package, and compile
        shell: bash
        run: |
          set -euo pipefail
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip check
          python -m pip wheel --no-deps . --wheel-dir dist
          gridpuzzle --help >/dev/null
          python -m compileall -q gridsolver tests scripts Examples run.py examples2.py

      - name: Run every bounded test
        run: >-
          python -X dev -m pytest -q
          tests
          -m "not slow"
          --durations=15

      - name: Verify and publish the tested source
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import subprocess

          expected = {
              ".github/patches/atomic-rule-rollback.patch",
              ".github/workflows/finalize-atomic-rule-rollback.yml",
              "gridsolver/abstract_grids/grid.py",
              "tests/test_rule_inputs.py",
          }
          changed = set(
              subprocess.check_output(
                  ["git", "diff", "--name-only", "HEAD"],
                  text=True,
              ).splitlines()
          )
          if changed != expected:
              raise SystemExit(
                  f"unexpected finalization diff: {sorted(changed ^ expected)}"
              )
          PY
          git add -A
          git diff --cached --check
          git config user.name "github-actions[bot]"
          git config user.email \
            "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Make trail rule rollback atomic"
          git push origin "HEAD:$GITHUB_REF_NAME"
