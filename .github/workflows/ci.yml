name: CI

on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PIP_DISABLE_PIP_VERSION_CHECK: "1"
  PYTHONHASHSEED: random
  PYTHONUNBUFFERED: "1"

jobs:
  core:
    name: Python 3.14 / Linux core
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python 3.14
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install project and development dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip check

      - name: Validate package and console script
        run: |
          python -m pip wheel --no-deps . --wheel-dir dist
          gridpuzzle --help

      - name: Compile Python sources
        run: python -m compileall -q gridsolver tests Examples run.py examples2.py

      - name: Run bounded core and regression tests
        run: >-
          python -X dev -m pytest -q
          tests/test_cli_and_loading.py
          tests/test_hardening.py
          tests/test_regressions.py
          tests/test_solver_api.py
          tests/test_basic.py
          tests/test_scale.py
          -m "not slow"
          -k "not test_parallel_trials_match_sequential"

      - name: Run guarantee-consumer metadata guard
        run: python -X dev -m pytest -q tests/test_rule_metadata.py

  examples:
    name: Python 3.14 / Representative examples
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python 3.14
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install project and development dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip check

      - name: Run representative end-to-end examples
        run: python -X dev -m pytest -q tests/test_examples_2.py

  windows-smoke:
    name: Python 3.14 / Windows smoke
    runs-on: windows-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python 3.14
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"
          cache: pip
          cache-dependency-path: pyproject.toml

      - name: Install project and development dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"
          python -m pip check

      - name: Smoke installed console script
        run: gridpuzzle --help

      - name: Compile Python sources
        run: python -m compileall -q gridsolver tests run.py examples2.py

      - name: Run portable regression tests
        run: >-
          python -X dev -m pytest -q
          tests/test_cli_and_loading.py
          tests/test_hardening.py
          tests/test_regressions.py
          tests/test_solver_api.py
